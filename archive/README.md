# Archived prototypes

This folder keeps older experimental code (`src/`, `builders/`, `test_env/`)
from early UROP iterations. The active runtime is the `linkground` package
and `scripts/` utilities at the repository root.
